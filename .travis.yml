language: python
python: 3.6
sudo: required

services:
  - docker

before_install:
  - docker pull ubuntu:16.04
  - docker ps -a

install:
  - pip install -q testinfra --use-wheel

script:
  - docker build -t teknoradio/youtube .
  - docker run --rm --entrypoint='' --name youtube teknoradio/youtube /bin/bash -c "which curl" | grep -q /usr/bin/curl
  - py.test --connection=docker -v

notifications:
  email : false
